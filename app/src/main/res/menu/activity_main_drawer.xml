<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/main_menu"
            android:icon="@android:drawable/ic_menu_camera"
            android:checked="true"
            android:title="精选" />
        <item android:id="@+id/fenlei_menu"
            android:icon="@android:drawable/ic_menu_gallery"
            android:title="分类" />
        <item android:id="@+id/shetuan_menu" android:icon="@android:drawable/ic_menu_send"
            android:title="社团与部门" />
    </group>

    <group>
        <item android:id="@+id/setting_menu"
            android:icon="@android:drawable/ic_menu_manage"
            android:title="设置" />
    </group>

    <item android:title="其他">
        <menu>
            <item android:id="@+id/info_menu"
                android:title="关于" />
            <item android:id="@+id/conversation_menu"
                android:title="联系我们" />
        </menu>
    </item>
</menu>
